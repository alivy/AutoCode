- 所有代码生成器均实现 IIncrementalGenerator 接口以支持增量编译，而非传统的 ISourceGenerator。
- 生成器通过自定义特性标记触发代码生成：[AutoInterface] 用于接口生成、[DotTemplate] 用于模板生成、[Mapper] 用于对象映射。
- 生成器统一使用 context.AddSource 将代码写入内存，不直接写磁盘文件，实现零文件写入。
- 测试项目采用 xUnit 配合 Microsoft.CodeAnalysis.Testing 对每个生成器进行单元测试验证。