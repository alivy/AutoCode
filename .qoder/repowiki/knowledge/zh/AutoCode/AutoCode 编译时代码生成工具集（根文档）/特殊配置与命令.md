构建入口为 `cd src && dotnet build AutoCode.sln`，测试为 `dotnet test AutoCode.sln`，NuGet 包通过 `dotnet build AutoCode.Extensions.SourceGenerator/AutoCode.SourceGenerator.Extensions.csproj -c Publish` 生成到 `src/.nuget/AM.AutoCode.{version}.nupkg`，发布需执行 `nuget push` 命令。