- Liquid 模板统一以 `// Generated by AutoCode CustomRecipe: <recipeName>` 注释开头，并通过 `{{ Namespace }}`、`{{ ClassName }}` 等变量注入上下文
- 模板参数通过 template.json 的 symbols 节点以布尔类型定义，默认值均为 true 以启用完整功能
- 生成的代码统一添加 `<auto-generated />` 标记，表明由工具自动生成不可手动修改
- 实体类通过组合 `[AutoEntity]`、`[AutoInterface]`、`[AutoIntercept]` 等特性声明式驱动代码生成，而非手写实现类