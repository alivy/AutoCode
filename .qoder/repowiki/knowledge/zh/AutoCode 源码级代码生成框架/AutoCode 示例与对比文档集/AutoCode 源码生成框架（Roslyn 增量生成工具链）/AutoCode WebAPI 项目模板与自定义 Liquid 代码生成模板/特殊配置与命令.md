通过 `dotnet new -i templates/autocode-webapi` 安装模板后，使用 `dotnet new autocode-webapi --name YourProject` 创建项目，可通过 `--enable-crud`、`--enable-intercept`、`--enable-validation` 参数开关生成能力；自定义 Liquid 模板需配合 autocode.json 配置使用。