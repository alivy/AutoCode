- 所有可被自动识别的代码实体通过 AutoCode.Model 中的特性（AutoController/AutoDTO/AutoEntity/AutoIntercept 等）标记，而非约定文件名。
- 生成产物遵循 autocode.json 中约定的命名模式（servicePattern/repositoryPattern/dtoSuffix）与输出命名空间规则。
- V1 与 V2 两套生成器并存于 AutoCode.Generators 下，通过版本化目录隔离并由测试分别覆盖，新能力优先在 V2 中实现。
- 自定义代码生成通过 customGenerators 声明式配置 + templates/*.liquid 模板实现，无需修改核心生成器即可扩展。