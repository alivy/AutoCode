- 所有可生成能力均通过 Attribute（Auto*Attribute）声明，生成器只扫描这些标记而不侵入业务代码。
- 新增功能以 IGenerationPlugin 形式实现为独立插件项目，通过 autocode.json 按需启用。
- 生成器同时维护 V1 与 V2 两套实现，由 V2Gate 依据配置选择，保证向后兼容。
- 编译期质量保障通过 Analyzer 实现，与 Source Generator 并列注册，统一输出 Diagnostic。