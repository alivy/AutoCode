本模块为纯文档型示例集合，不包含可执行代码，由根目录 README.md 作为入口导航，四个子目录按学习路径组织：01-QuickStart 展示 [AutoEntity] 一次性生成接口、DTO、Mapper、Validator、Repository、Controller 和 DI 注册；02-InterceptAOP 用 [AutoIntercept] 替代 Castle DynamicProxy 实现编译时 AOP；03-TypedMethodHandler 演示强类型 IMethodHandler<TArgs, TResult> 拦截器及自动生成的 Args record；04-AopComparison 提供与 Castle/PostSharp 的性能基准对比表。每个示例以 Before/After 对照形式呈现，生成产物统一位于 obj/Debug/net8.0/generated/ 下，遵循 .g.cs 命名约定。