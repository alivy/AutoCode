// <auto-generated />
// Generated by AutoCode CustomRecipe: repositoryPattern
// Template: Repository.liquid
using System;
using System.Collections.Generic;
using System.Linq;

namespace {{ Namespace }}
{
    /// <summary>
    /// 内存 Repository 实现 - 自动为 {{ ClassName }} 生成 CRUD 仓储。
    /// </summary>
    public class {{ ClassName }}Repository : I{{ ClassName }}Repository
    {
        private readonly Dictionary<int, {{ ClassName }}> _store = new Dictionary<int, {{ ClassName }}>();
        private int _nextId = 1;

        public {{ ClassName }}? GetById(int id)
        {
            return _store.TryGetValue(id, out var entity) ? entity : null;
        }

        public IReadOnlyList<{{ ClassName }}> GetAll()
        {
            return _store.Values.ToList();
        }

        public {{ ClassName }} Create({{ ClassName }} entity)
        {
            var id = _nextId++;
            _store[id] = entity;
            return entity;
        }

        public bool Update({{ ClassName }} entity)
        {
{% for prop in Properties %}
{% if prop.Name == "Id" %}
            // 以 Id 为键更新
            // _store[entity.Id] = entity;
            return true;
{% endif %}
{% endfor %}
            return false;
        }

        public bool Delete(int id)
        {
            return _store.Remove(id);
        }
    }

    /// <summary>
    /// {{ ClassName }} 仓储接口
    /// </summary>
    public interface I{{ ClassName }}Repository
    {
        {{ ClassName }}? GetById(int id);
        IReadOnlyList<{{ ClassName }}> GetAll();
        {{ ClassName }} Create({{ ClassName }} entity);
        bool Update({{ ClassName }} entity);
        bool Delete(int id);
    }
}
